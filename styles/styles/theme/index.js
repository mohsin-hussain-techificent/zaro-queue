import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    // primary: {
    //   main: "#e2765a",
    // },
    secondary: {
      main: "#f44336",
    },
    custom: {
      dark: "#1B263D",
      lightBluBackground: "#F1F9FF",
      darkBlueBackgroundColor: "#1B263D",
    },
  },
});
export default theme;
